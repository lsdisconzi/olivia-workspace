{
  "id": "trampoline",
  "name": "Trampoline",
  "icon": "🤸",
  "category": "prop",
  "tags": ["playground","garden","fun"],
  "desc": "Trampoline with bounce animation",
  "params": {
    "x": { "type": "x", "default": "w*0.7" },
    "y": { "type": "y", "default": "bY" },
    "scale": { "type": "number", "default": 1 }
  },
  "code": "const s=ctx.scale||1; const x=ctx.x||0; const y=ctx.y||0; const tr=30; const bounce=Math.sin(mt*3)*2; svg += `<g transform=\"translate(${x},${y}) scale(${s})\">`; svg += `<line x1=\"${-tr+5}\" y1=\"0\" x2=\"${-tr+10}\" y2=\"${-18}\" stroke=\"#666\" stroke-width=\"2\"/>`; svg += `<line x1=\"${tr-5}\" y1=\"0\" x2=\"${tr-10}\" y2=\"${-18}\" stroke=\"#666\" stroke-width=\"2\"/>`; svg += `<ellipse cx=\"0\" cy=\"${-18+bounce}\" rx=\"${tr}\" ry=\"6\" fill=\"#2A2A2A\" stroke=\"#444\" stroke-width=\"${ol*0.5}\"/>`; for(let i=0;i<6;i++){ const sa=-Math.PI*0.8+i*(Math.PI*1.6/5); svg += `<line x1=\"${Math.cos(sa)*(tr-2)}\" y1=\"${-18+bounce}\" x2=\"${Math.cos(sa)*(tr+3)}\" y2=\"${-14}\" stroke=\"#888\" stroke-width=\"1\"/>`; } svg += `</g>`; return svg;"
}