{
  "id": "mushroom_patch",
  "name": "Glowing Mushroom Patch",
  "icon": "🍄",
  "category": "prop",
  "tags": ["forest", "magic", "glow", "plant"],
  "desc": "Row of multi-color mushrooms with pulsing magical glow",
  "params": {
    "x":     { "type": "x",     "default": 0 },
    "y":     { "type": "y",     "default": "bY+3" },
    "w":     { "type": "number","default": "w" },
    "count": { "type": "number","default": 6 }
  },
  "code": "const ax = ctx.x ?? 0; const ay = ctx.y ?? 260; const aw = ctx.w ?? 400; const count = ctx.count ?? 6;\nconst cols = ['#FF6B9D','#00CED1','#9370DB','#FF8C42','#4ECDC4','#DA70D6','#FFB347','#87CEEB'];\nfor (let i = 0; i < count; i++) {\n  const mx = ax + (i+0.5)*(aw/count);\n  const my = ay + Math.sin(i*2.1)*5;\n  const mh = 7 + Math.sin(i*2.1)*2;\n  const g = Math.sin(mt*1.5+i*1.1)*0.5+0.5;\n  const c = cols[i % cols.length];\n  svg += `<rect x=\"${mx-1.5}\" y=\"${my-mh}\" width=\"3\" height=\"${mh}\" rx=\"1\" fill=\"#E8D8C0\"/>`;\n  svg += `<ellipse cx=\"${mx}\" cy=\"${my-mh}\" rx=\"${6+Math.sin(i*1.7)*2}\" ry=\"${3.5+Math.sin(i*1.3)}\" fill=\"${c}\"/>`;\n  svg += `<circle cx=\"${mx-2}\" cy=\"${my-mh-1}\" r=\"0.8\" fill=\"#FFF\" opacity=\"0.5\"/>`;\n  svg += `<circle cx=\"${mx}\" cy=\"${my-mh}\" r=\"${10+g*5}\" fill=\"${c}\" opacity=\"${0.03+g*0.04}\"/>`;\n}"
}
