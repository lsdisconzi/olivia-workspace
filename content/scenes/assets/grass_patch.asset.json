{
  "id": "grass_patch",
  "name": "Grass Patch (wavy)",
  "icon": "🌱",
  "category": "environment",
  "tags": ["ground", "plant", "nature"],
  "desc": "Strip of wavy grass tufts with subtle sway; place along the horizon line",
  "params": {
    "x":     { "type": "x",     "default": 0 },
    "y":     { "type": "y",     "default": "bY-3" },
    "w":     { "type": "number","default": "w" },
    "count": { "type": "number","default": 18 },
    "color": { "type": "color", "default": "#3A6A3A" }
  },
  "code": "const ax = ctx.x ?? 0; const ay = ctx.y ?? 260; const aw = ctx.w ?? 400; const count = ctx.count ?? 18; const col = ctx.color || '#3A6A3A';\nfor (let i = 0; i < count; i++) {\n  const gx = ax + (i+0.5)*(aw/count);\n  const gy = ay + Math.sin(i*1.9+mt*0.3)*6;\n  svg += `<ellipse cx=\"${gx}\" cy=\"${gy}\" rx=\"${7+Math.sin(i*1.3)*3}\" ry=\"2.5\" fill=\"${col}\" opacity=\"0.55\"/>`;\n}"
}
