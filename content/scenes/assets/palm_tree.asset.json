{
  "id": "palm_tree",
  "name": "Palm Tree",
  "icon": "🌴",
  "category": "environment",
  "tags": ["tropical","beach","plant"],
  "desc": "Swaying palm tree with fronds",
  "params": {
    "x": { "type": "x", "default": "w*0.3" },
    "y": { "type": "y", "default": "bY-10" },
    "scale": { "type": "number", "default": 1 }
  },
  "code": "const s=ctx.scale||1; const x=ctx.x||0; const y=ctx.y||0; const sway=Math.sin(mt*0.8+2)*0.3; svg += `<g transform=\"translate(${x},${y}) scale(${s})\">`; svg += `<rect x=\"-2\" y=\"-45\" width=\"5\" height=\"45\" fill=\"#6D4C41\"/>`; for(let j=0;j<6;j++){ const angle = j*1.05 - 1.57 + sway; const len = 25 + Math.sin(j*0.5)*5; const fx = 2.5 + Math.cos(angle)*len; const fy = -45 + Math.sin(angle)*len; svg += `<line x1=\"2.5\" y1=\"-45\" x2=\"${fx}\" y2=\"${fy}\" stroke=\"#1B5E20\" stroke-width=\"4\" stroke-linecap=\"round\"/>`; } svg += `</g>`; return svg;"
}